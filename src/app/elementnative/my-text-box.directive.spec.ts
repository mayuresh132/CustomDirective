import { MyTextBoxDirective } from './my-text-box.directive';

describe('MyTextBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new MyTextBoxDirective();
    expect(directive).toBeTruthy();
  });
});
