import { NativeEleRefDirective } from './native-ele-ref.directive';

describe('NativeEleRefDirective', () => {
  it('should create an instance', () => {
    const directive = new NativeEleRefDirective();
    expect(directive).toBeTruthy();
  });
});
